# Blender Hub

## Installation

### Windows

To run `pywebview` in Windows, `pythonnet 2.5` is needed but is not compatible with `python 3.7` or earlier versions.

The `pythonnet` package has been updated to `3.1.0rc0` but it would be a good idea to check if Blender Hub works with early versions of `python3` as well.